# Main CLI argparse baseline

The user stated they now understand the broad `main.py` usage and CLI argument
flow after covering parser/subparser, `args`, `args.preset`, and `config`.
Future lessons can treat CLI parsing as a usable baseline and focus on runtime
data contracts rather than re-explaining argparse from scratch.
